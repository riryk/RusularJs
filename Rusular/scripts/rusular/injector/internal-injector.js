
function createInternalInjector(cache, factory) {

    function invoke() {
    }

    function instantiate() {
    }

    function getService() {
    }

    return {
        invoke: invoke,
        instantiate: instantiate,
        get: getService
    };
}